import React from "react";

const Button = () => {
  return (
    <button className="border hover:bg-white hover:text-slate-900 rounded-r-full px-2 font-semibold">
      Search
    </button>
  );
};

export default Button;
